# 노트앱 — 기일노트 · 조사노트 · 상담노트를 직접 호스팅

세 앱을 claude.ai 아티팩트가 아니라 **내 GitHub Pages 사이트**로 띄운다.
아티팩트는 claude.ai 틀(iframe) 안에 뜨는 화면이라 마이크가 열리지 않고, 열 때마다 claude.ai 로그인과 네트워크가 필요했다.
직접 호스팅하면 최상위 문서가 되어 **마이크가 열리고**, 서비스워커가 앱 껍데기를 캐시해 **네트워크 없이도 열리며**, 로그인이 없다.

주소는 `주소.md` (첫 배포 뒤 생김). 각 앱의 사용법.md 「앱 주소」 줄도 배포 때 자동으로 바뀐다.

---

## 처음 한 번 — 배포

```
python "E:\노트앱\배포.py"
```

1. 빌드 → `docs\`
2. GitHub 로그인이 없으면 `gh auth login` 이 뜬다. 브라우저에서 코드를 입력한다. 계정이 없으면 그 화면에서 **Sign up → Continue with Google** 로 1분.
3. 저장소 `notedesk` 를 **공개(public)** 로 만들고 푸시한다 — 무료 계정의 Pages 는 공개 저장소만 된다. 올라가는 것은 앱 코드뿐이다. 사건 자료(팩)는 드라이브로만 다니고 앱 안(아이패드 IndexedDB)에만 저장된다.
4. Pages 를 켜고(main / docs) 사이트가 뜰 때까지 기다린다 (첫 배포 1~2분).
5. 주소를 찍어 준다. `https://<GitHub아이디>.github.io/notedesk/` 아래 `gijil/` `josa/` `sangdam/`.

## 아이패드에서

1. 사파리로 앱 주소를 연다 (첫 화면에서 골라도 된다).
2. 공유 → **홈 화면에 추가**. 앱마다 따로 넣는다 (이름·아이콘이 다르다).
3. 한 번 열어 두면 그 뒤로는 네트워크 없이도 열린다. 새 판을 올리면 네트워크가 있을 때 자동으로 바뀐다 (왼쪽 위 판 표시로 확인).
4. **상담노트 녹음**: 첫 녹음 때 마이크 허용을 묻는다. 거부했다면 설정 › Safari › 마이크, 홈 화면 앱은 설정의 「상담노트」 항목.
5. **내보내기·녹음 내려받기**: 공유 시트가 뜬다 → 「파일에 저장」 또는 드라이브 앱.

claude.ai 아티팩트 판에 있던 노트·형광펜은 옮겨지지 않는다 (저장소가 사이트마다 따로다). 필요하면 옛 앱에서 내보내기로 md 를 뽑아 두고, 팩은 새 앱에 다시 넣는다.

## 앱을 고친 뒤

앱 원본은 `src\` 의 세 html 이다 (아티팩트에 올리던 것과 같은 판).
고치면 다시 `python 배포.py` — 빌드·커밋·푸시까지 한 번에. 판 번호(`Hearing Desk · 9.8d` 같은 머리말)는 원본에서 올린다.

| 파일 | 하는 일 |
|---|---|
| `빌드.py` | src → docs (PWA 머리말·아이콘·manifest·서비스워커·내려받기 표준화) |
| `배포.py` | 빌드 + git 커밋·푸시 + Pages 켜기 + 주소 기록 |
| `미리보기.py` | docs 를 http://localhost:8765 로 띄워 배포 전에 본다 |
| `vendor\jszip.min.js` | zip 풀기 라이브러리 로컬 사본 (오프라인용) |

## 아티팩트 판과 다른 점

- `window.claude.downloads` 대신 표준 내려받기(공유 시트 → 파일/드라이브)
- jszip 을 CDN 이 아니라 사이트 안에서 읽는다
- 서비스워커 `sw.js`: 화면(html)은 네트워크 먼저(3초) → 안 오면 캐시, 나머지는 캐시 먼저. 판이 바뀌면 옛 캐시는 지운다
